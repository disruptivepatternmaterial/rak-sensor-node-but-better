# Open items live in GitHub Issues

This file used to be the running list. It is not anymore, because a list in the repo and a
list in the tracker drift apart and then neither is trustworthy.

**→ https://github.com/disruptivepatternmaterial/rak-sensor-node-but-better/issues**

Labels that matter:

| Label | Meaning |
|---|---|
| `blocks-first-uplink` | Nothing transmits until this is done |
| `needs-hardware` | Cannot be answered until the node is on a bench or a pole |
| `blocks-deployment` | Must close before it goes in the woods |
| `needs-decoder-change` | Requires a paired change to the TTN formatter |

Anything that would otherwise become an "oh, one more thing" gets filed as an issue at the
moment it is noticed. If it is worth mentioning, it is worth a number.
